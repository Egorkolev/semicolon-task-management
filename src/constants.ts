export enum Status {
    ALL="ALL",
    PENDING="PENDING",
    IN_PROGRESS="IN_PROGRESS",
    COMPLETE="COMPLETE"
}

export enum Priority {
    LOW="LOW",
    MIDDLE="MIDDLE",
    HIGH="HIGH"
}

export enum CharactersLimits {
    TaskNameMax = 50,
    TextAreaMax = 200,
    UserNameMin = 3,
    InputMax = 20,
    InputMin = 2,
    PasswordMin = 8,
}